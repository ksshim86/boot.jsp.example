spring:
  mvc:
    view:
      prefix: /WEB-INF/jsp/
      suffix: .jsp
      
  datasource:
    driver-class-name: oracle.jdbc.OracleDriver
    url: jdbc:oracle:thin:@ip:port:sid
    username: id
    password: pw

mybatis:
    config-location: classpath:mybatis-config.xml

logging:
  level:
    root: info 
    com.boot.example.mapper: trace
